import TournamentDashboard from "@/components/tournament-dashboard";

export const metadata = {
  title: "MatchDay Command · Tournament",
};

export default function TournamentPage() {
  return <TournamentDashboard />;
}
